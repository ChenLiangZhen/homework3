import {Text} from "react-native";
import {BaseLayouts} from "../components/BaseComponents";

const Home = ({navigation}) => {
	return(
		<>
			<BaseLayouts h={100} w={100} bg="green">

			</BaseLayouts>

			<Text style={{ fontSize: 24 }}>
				sdvbsvjl
			</Text>
		</>

	)
}

export default Home